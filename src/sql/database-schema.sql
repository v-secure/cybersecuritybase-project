CREATE TABLE Account (
    id varchar(9) PRIMARY KEY,
    username varchar(200),
    password varchar(200)
);

CREATE TABLE Signup (
    id varchar(9) PRIMARY KEY,
    name varchar(200),
    address varchar(200)
);
